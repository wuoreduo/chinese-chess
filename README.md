# 中国象棋

## 🎮 启动方式

### 方式一：GUI 控制器（推荐 ⭐）

图形界面，直观控制服务器，实时显示状态

**Linux/macOS:**
```bash
python3 controller.py
```

**Windows:**
```
双击 controller.pyw（无控制台窗口）
或运行：python controller.py
```

> 💡 **为什么有两个文件？**
> - `.py` - 会显示控制台窗口
> - `.pyw` - 不显示控制台窗口（Windows 专用）
> 
> 详见 `WINDOWS.md`

**功能:**
- 🟢 一键启动/停止/重启
- 📊 实时显示状态（PID、运行时间、活跃游戏数）
- 📋 查看日志
- 🔗 点击链接打开浏览器

**界面预览:**
```
┌─────────────────────────────────────┐
│       中国象棋游戏控制器             │
├─────────────────────────────────────┤
│  服务器状态                         │
│  ┌─────────────────────────────┐   │
│  │ 状态：🟢 运行中              │   │
│  │ PID: 2817826                 │   │
│  │ 活跃游戏：7                  │   │
│  │ 运行时间：00:06              │   │
│  └─────────────────────────────┘   │
│                                     │
│  [启动]     [停止]     [重启]       │
│                                     │
│  访问地址：http://localhost:5000    │
│                                     │
│        [查看日志]                   │
│                                     │
│  最近日志：                         │
│  ┌─────────────────────────────┐   │
│  │ Serving Flask app 'server'  │   │
│  │ Running on port 5000...     │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

### 方式二：命令行管理脚本

**Linux/macOS:**
```bash
./manage.sh start    # 启动
./manage.sh stop     # 停止
./manage.sh restart  # 重启
./manage.sh status   # 状态
./manage.sh logs     # 日志
```

**Windows:**
```cmd
manage.bat start
manage.bat stop
manage.bat restart
manage.bat status
manage.bat logs
```

---

### 方式三：快速启动

**Linux/macOS:**
```bash
./start.sh
```

**Windows:**
```
双击 start.bat
```

---

### 方式四：直接启动

```bash
python3 server.py
```
按 `Ctrl+C` 停止

---

## 🌐 访问

浏览器打开：**http://localhost:5000**

---

## 📡 curl API 示例

```bash
# 创建游戏 (pvp|pvai|aivai)
curl -X POST http://localhost:5000/api/games \
  -H "Content-Type: application/json" \
  -d '{"type": "pvai"}'

# 走棋
curl -X POST http://localhost:5000/api/games/1/move \
  -H "Content-Type: application/json" \
  -d '{"from": [6, 4], "to": [5, 4]}'

# 获取游戏状态
curl http://localhost:5000/api/games/1

# 悔棋
curl -X POST http://localhost:5000/api/games/1/undo

# 获取 FEN
curl http://localhost:5000/api/games/1/fen

# 删除游戏
curl -X DELETE http://localhost:5000/api/games/1
```

---

## 🎯 游戏模式

| 模式 | 说明 |
|------|------|
| 🏆 人人对战 | 两名玩家轮流下棋 |
| 🤖 人机对战 | 玩家 vs AI（Minimax 算法） |
| ⚔️ 机机对战 | AI vs AI 自动对弈 |

---

## 📁 项目结构

```
中国象棋/
├── controller.py      # GUI 控制器
├── controller.pyw     # Windows 无窗口版本
├── manage.sh          # Linux 管理脚本
├── manage.bat         # Windows 管理脚本
├── start.sh           # Linux 快速启动
├── start.bat          # Windows 快速启动
├── server.py          # Flask 后端
├── game.py            # 象棋规则
├── ai.py              # Minimax AI
├── database.py        # SQLite 数据库
├── requirements.txt   # 依赖
├── templates/
│   └── index.html     # 前端页面
└── static/
    ├── style.css      # 样式
    └── game.js        # 前端逻辑
```

---

## 🪟 Windows 原生运行

本项目无需 Docker 或 WSL，可直接在原生 Windows 运行：

1. 安装 [Python 3.8+](https://www.python.org/downloads/)
2. 复制整个项目到 Windows
3. 双击 `controller.pyw`
4. 点击"启动"按钮
5. 浏览器访问 http://localhost:5000

---

## 📦 依赖安装

```bash
pip install -r requirements.txt
```

包含：
- Flask (Web 框架)
- Flask-SocketIO (WebSocket)
- psutil (进程管理)
